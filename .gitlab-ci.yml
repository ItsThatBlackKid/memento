image: docker:latest
variables: 
  DOCKER_TLS_CERTDIR: "/certs"
services: 
  - docker:dind
  
before_script:
  - docker info

build:
  stage: build
  script:
    - sudo apt install docker-compose
    - docker-compose build --no-cache
 
deploy:
 stage: deploy
 script:
   - sudo apt install docker-compose
   - docker-compose up -d
