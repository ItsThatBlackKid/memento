image: docker:19.03.01

variables: 
  DOCKER_TLS_CERTDIR: "/certs"
services: 
  - docker:19.03.1-dind
  
before_script:
  - docker info

build:
  stage: build
  script: 
    - docker-compose build --no-cache
 
 deploy:
 stage: deploy
 script:
  - docker-compose up -d
