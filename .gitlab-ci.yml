before_script:
  - docker version
  - docker-compose version

variables:
  GIT_SUBMODULE_STRATEGY: recursive

build:
  script:
    - docker-compose build --no-cache
  stage: build
  tags:
    - memento

deploy:
  script:
    - docker-compose up -d
  stage: deploy
  tags:
    - memento

